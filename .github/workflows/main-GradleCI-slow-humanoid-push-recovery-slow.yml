name: CI - main - slow humanoid-push-recovery

on:
  schedule:
    # Run every day at midnight. Add another cron entry to run at a different time.
    - cron: '0 0 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-slow-humanoid-push-recovery:
    uses: ihmcrobotics/valkyrie/.github/workflows/main-GradleCI-base.yml@main
    with:
      test-category: "humanoid-push-recovery-slow"

