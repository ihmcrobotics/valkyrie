name: CI - main - slow humanoid-flat-ground

on:
  schedule:
    # Run every day at midnight. Add another cron entry to run at a different time.
    - cron: '0 0 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-slow-humanoid-flat-ground:
    uses: ihmcrobotics/valkyrie/.github/workflows/version-dep-GradleCI-base.yml@main
    with:
      test-category: "humanoid-flat-ground-slow"

